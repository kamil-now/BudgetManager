namespace BudgetManager.Domain.Interfaces;

public interface IDomainEvent
{
    // This interface is intentionally left empty.
    // It serves as a marker interface for domain events in the domain layer.
}
